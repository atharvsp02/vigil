{
  "name": "vigil",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "pretypecheck": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "pretest": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@truefoundry/trueforge": "0.1.4",
    "typescript": "^5.7.2"
  }
}
